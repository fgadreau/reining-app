# Standalone Supabase SQL Archive

These SQL files are historical ShowScore standalone-database artifacts.
They are kept for reference only and must not be applied to the shared
HSP/ShowScore Supabase project.

The authoritative shared schema lives in the HorseShowPlatform Supabase
migrations. Use those migrations for production changes.
